# SUB Tool

多軌時間軸上字幕工具的領域詞彙表。**只放詞彙定義，不放實作細節**——
實作寫在 `docs/技術架構說明.md`，這裡只回答「這個詞指的是什麼」。

新增詞彙前先確認它是本專案特有的概念，而不是一般的程式概念。

## 畫布

**專案畫布**：
所有片段、圖片疊層與 PiP 進行合成的那張畫面，尺寸由開啟的第一個素材決定。
預覽與匯出用的是同一張，這是「三路一致」的基礎。
_Avoid_: 專案解析度、輸出解析度、影片尺寸

**字幕畫布**：
字幕排版所依據的虛擬畫面，**固定 1920×1080**，與專案畫布無關。
字級與位置百分比都相對於它，實際顯示大小由 libass 依輸出高度等比縮放。
_Avoid_: PlayRes、字幕解析度

**交付解析度**：
一份交付實際寫進檔案的畫面尺寸。與專案畫布同比例時純粹是等比縮放；
改變比例則牽涉裁切或黑邊，是另一件事。
_Avoid_: 匯出解析度、輸出尺寸

## 字幕

**ASS 往返**：
把 SUB Tool 匯出的 ASS 再匯入專案；自產檔應還原原本的字幕內容、時間軸影格與視覺樣式，
外部 ASS 則盡可能轉換其可表示的靜態樣式。
_Avoid_: ASS 匯入、字幕重建

## 匯出

**交付規格**：
一份輸出檔案的完整描述——格式、交付解析度、位元率、是否燒入時間碼、音訊編組。
一次匯出可以有多份交付規格，彼此獨立。
_Avoid_: 匯出設定、輸出設定、preset

**匯出工作**：
一份交付規格加上送出當下凍結的專案快照。送出之後與專案完全脫鉤，
之後怎麼改專案都不影響已送出的工作。
_Avoid_: 匯出任務、job、輸出項目

**匯出佇列**：
所有匯出工作的排程與執行狀態。工作可個別暫停、停止，並行度可調。
_Avoid_: 輸出佇列、批次匯出、背景工作

## 素材

**母素材**：
使用者原本提供的那個檔案。匯出**只准**讀它。
_Avoid_: 原始檔、來源檔

**Proxy**：
為了流暢預覽而轉出的低解析度影片。**絕不可進入匯出**。
_Avoid_: 代理檔、預覽檔

## 音訊

**來源聲道**：
母素材內可獨立讀取的一個聲道，以從 1 開始的號碼識別；它可映射到專案音軌，
但本身不是專案音軌或輸出 stream。
_Avoid_: 原始軌道、輸入軌道、聲道

**專案音軌**（程式碼名稱：audio bus）：
一條單聲道的專案輸出（A1、A2…）。可由多個素材聲道混音而成。使用者文件稱「專案音軌」；
程式碼與維護文件談資料結構時可稱 `bus`，兩者指的是同一層。
_Avoid_: 把素材聲道或輸出 stream 也叫成專案音軌

**輸出 stream**：
影片容器內的一條音訊串流，由若干條專案音軌依 Mono／Stereo／LtRt／5.1 編組而成。
_Avoid_: 聲道組、音訊軌
