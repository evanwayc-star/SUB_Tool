# SUB Tool 領域詞彙

這裡只定義「詞指什麼」，不寫實作。實作見 [技術架構說明](docs/技術架構說明.md)，決策原因見 [ADR](docs/adr/)。

## 畫面

**專案畫布**：影片、圖片與疊層共同合成的畫面；尺寸由第一個有效視訊素材建立。預覽與交付共用。避免和字幕畫布混稱「影片尺寸」。

**字幕畫布**：字幕排版的固定 1920×1080 座標系。實際顯示依畫面高度等比縮放。

**交付解析度**：某一份輸出檔實際寫入的寬高。同一專案可建立多份不同交付解析度。

## 時間與播放

**時間軸時間**：UI、字幕、片段、備註、In／Out 與交付共同使用的專案時間。

**來源時間**：某個母素材內部的時間。只在 HTML video、mpv、WebCodecs 或來源音訊邊界使用。

**播放目標**：使用者要求播放器前往的時間軸位置。解碼未完成前，不代表畫面已抵達。

**呈現位置**：目前真正可見畫格所對應的時間軸位置。解碼追趕時可以落後播放目標。

**逐格**：以目前呈現位置為基準，移動一個精確影格；不是在原始播放器浮點時間上加減近似秒數。

## 時間軸

**時間軸手勢**：從 pointer down 到 commit 或 cancel 的完整互動；目標群組與修飾鍵語意在手勢中不變。

**手勢預覽**：尚未提交、可完整還原的暫時位置或範圍。

**手勢提交**：把最後預覽原子套用到專案，最多建立一筆 Undo。

**鎖定軌**：保護軌內內容不被選取或修改的軌道。鎖定不封鎖播放點：軌道區域仍可點擊或拖曳 seek。

## 字幕

**生效樣式**：軌道樣式、常用樣式與逐句覆蓋合成後的最終字幕樣式；HTML、mpv 與燒錄都使用它。

**逐句覆蓋**：只屬於一條字幕、與軌道樣式不同的欄位，例如單句位置或旋轉。

**ASS 往返**：把 SUB Tool 匯出的 ASS 再匯入；自產檔應還原文字、影格與可表示的視覺樣式。

**語音辨識**：由聲音產生文字與時間碼。

**文本匹配**：使用者提供已校對、已逐行分句的原稿，程式只分析每行時間。文字、標點、行界與順序不被改寫。

**無時間碼字幕**：保留原稿文字但沒有足夠證據給出可靠 In／Out 的字幕，資料上為 `timed:false`。

**辨識音訊來源**：本次辨識實際分析的來源聲道集合。它不改變播放混音、專案配線或交付。

**辨識工作**：一次已接受且已凍結素材區間、來源聲道、原稿與格網的背景工作。視窗開關不會建立新工作。

**對齊品質**：文本匹配每行時間證據的可信度：可靠、需校對或無時間碼。它不是工作執行狀態。

## 素材

**母素材**：使用者提供的原始媒體檔。正式交付只准讀母素材。

**Proxy**：為流暢預覽建立的低解析度或短 GOP 影片。絕不可進入交付。

**媒體快取**：可重建的 Proxy、波形與逐聲道音訊；不是專案資料或母素材。

## 音訊

**來源聲道**：母素材內可獨立讀取的 stream／channel 座標。

**專案音軌**（程式碼：audio bus）：時間軸上的單聲道 A1、A2…；可混合多個來源聲道。

**輸出 stream**：交付容器內的一條音訊串流，由專案音軌編組成 Mono、Stereo、LtRt 或 5.1。

## 交付

**交付規格**：一個輸出檔的格式、解析度、bitrate、TC 與音訊編組。

**匯出工作**：一份交付規格加上送出當下的凍結專案快照。後續編輯不影響它。

**匯出佇列**：所有匯出工作的持久化排程與執行狀態。

**輸出 lease**：保證同一輸出路徑同時只有一個 live 工作可以寫入的跨程序鎖。
