參考資料/
test-media/

# .claude/ 本身【不擋】——launch.json 等設定要共用給多個 AI 工具（見 AGENTS.md §0）。
# 只擋本機專屬的那一份：settings.local.json 存的是這台機器的權限白名單，
# 裡面含使用者家目錄與樣本檔的絕對路徑，且每次授權都會變動。
.claude/settings.local.json

# 建置產物（npm run build / npm run dist 產生，不進版控）
# dist/ 曾在 898d092 被移除追蹤，但當時漏了補這一行，
# 導致它每次 build 後又冒出來、隨時可能被 git add -A 掃回來。
dist/
release/
*.mxf
*.mov
*.mp4
*.mkv
*.mp3
*.wav
*.m4a


# Dependencies and executables
node_modules/
electron/ffmpeg/
electron/mpv/
*.exe
.config/
