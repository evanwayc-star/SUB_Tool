# CLAUDE.md

本專案的 AI 助理規則統一寫在 **[`AGENTS.md`](AGENTS.md)**，請直接讀那一份。

之所以只放指標而不複製一份：本專案由多個 AI 工具共同維護
（Antigravity IDE、Claude Code、OpenAI Codex），規則若有兩份就會開始漂移——
那正是這個專案已經發生過的問題（見 `AGENTS.md` §0）。

**最重要的三條**（細節在 `AGENTS.md`）：

1. **絕對不要對版號做全域字串取代**——升版只改 `package.json` 與 `package-lock.json`。
   文件裡的 `## [v4.31.2]`、「v4.27 起才開始壞」是**歷史事實**，不是待更新的欄位。
2. 改程式碼前先讀 [`docs/技術架構說明.md`](docs/技術架構說明.md) **§0「鐵律」**——
   那些是違反後會**靜默**壞掉的不變量（畫面正常、匯出是錯的）。
3. **Git 提交訊息一律使用繁體中文。**
