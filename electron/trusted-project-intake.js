/* ==============================================================================
   SUB Tool — Trusted Project Intake Adapter
   ==============================================================================
   核心實作已深化至 electron/project-file-authority-engine.js。
   ============================================================================== */
'use strict';

const { createTrustedProjectIntake } = require('./project-file-authority-engine');

module.exports = { createTrustedProjectIntake };
